import { build } from 'esbuild';
import { fileURLToPath } from 'node:url';
import path from 'node:path';

const projectRoot = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');

await build({
  entryPoints: [path.join(projectRoot, 'src/abilities/serverRuntimeEntry.ts')],
  outfile: path.join(projectRoot, 'shared/abilities/runtime.mjs'),
  bundle: true,
  platform: 'neutral',
  format: 'esm',
  target: 'es2022',
  legalComments: 'none',
  sourcemap: false,
  banner: {
    js: '// Generated by npm run build:abilities. Do not edit by hand.',
  },
});
